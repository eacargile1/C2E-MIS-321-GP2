# API base URL (no trailing slash). Default matches api/Properties/launchSettings.json http profile.
# For Heroku: use https://your-team-api.herokuapp.com — copy to .env.local (gitignored) and restart Vite.
VITE_API_BASE_URL=http://localhost:5028
